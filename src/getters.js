export const value = state => {
    return state.value;
};

export const productName = state => {
    return state.productName;
};

export const productCost = state => {
    return state.productCost;
};

export const cartLength = state => {
    return state.cartLength;
};